# Benchmark result bundles

Generated local results are ignored. Release qualification bundles are stored
under a release-specific directory or attached immutably to the release.
Use `python -m benchmarks.runner.cli --help` for the paired runner.
