[package]
name = "tectdist-core"
version = "0.2.2"
edition = "2021"
license = "AGPL-3.0-only"

[lib]
path = "src/lib.rs"

[features]
# Oracle-comparison tooling (plan §2.3): enables the OracleRunner surface.
# Never enabled for production builds; BasicTeX stays oracle-only.
oracle-tools = []
